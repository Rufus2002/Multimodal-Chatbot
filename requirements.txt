torch  # Let the installer choose the most compatible version
streamlit==1.24.0
transformers==4.30.2
requests
Pillow==9.5.0  # or Pillow==8.4.0 if issues persist
tokenizers==0.13.3  # This version has pre-built wheels for many environments.
