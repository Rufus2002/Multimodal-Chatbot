Flask==2.3.2
pip==24.2
transformers==4.30.2
torch  # Let the installer choose the most compatible version
streamlit==1.24.0
groq
Pillow  # or Pillow==8.4.0 if issues persist
tokenizers==0.13.3
